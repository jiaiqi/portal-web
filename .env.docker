# ============================================
# Docker 部署环境变量配置
# 使用前请复制此文件为 .env 并修改敏感信息
# cp .env.docker .env
# ============================================

# MySQL 配置
MYSQL_ROOT_PASSWORD=your_secure_root_password
MYSQL_DATABASE=nest_admin
MYSQL_USER=nest_admin
MYSQL_PASSWORD=your_secure_password

# Redis 配置
REDIS_PASSWORD=your_redis_password

# Server 后端配置
NODE_ENV=production
JWT_SECRET=your_very_long_random_jwt_secret_key_change_this

# API 基础路径（供前端使用）
API_BASE_URL=http://localhost/api

# 门户网站 API 地址（容器内部通信）
NUXT_PUBLIC_API_BASE_URL=http://nginx/api
