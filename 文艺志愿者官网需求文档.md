# 文艺志愿者官网（地方分会）需求文档

## 一、项目概述

**项目名称：** 文艺志愿者官网 - 地方分会  
**项目类型：** 政府/协会性质官方网站  
**技术栈：** Vue3 + NestJS（基于现有CMS后台管理系统系统）

## 二、功能模块总览

### 2.1 首页功能模块

#### 基础模块（原有）
- 公告公示展示区
- 要闻展示区
- 资讯展示区
- 友情链接展示区
- 导航菜单
- 底部链接
- 网站总访问量统计
- 分模块访问量统计
- 公众号二维码展示

#### 新增模块（根据截图分析补充）
- **顶部快捷链接** - 中国文艺网、中国文联等链接
- **大幅轮播图** - 首页Banner展示重要活动
- **专题/精神学习区** - 重要思想、会议精神展示
- **协会概况侧边栏** - 协会简介快速入口
- **快捷入口区** - 重要平台链接
- **专题推荐区** - 侧边栏专题展示
- **活动展示区** - 品牌活动图文展示
- **底部完整导航** - 网站地图式导航

### 2.2 首页功能模块清单对比表

| 模块 | 原有 | 新增 | 状态 |
|------|------|------|------|
| 顶部导航 | ✅ | 二级导航、快捷链接 | 需完善 |
| 轮播图 | ⚪️ | 大幅Banner轮播 | 新增 |
| 专题/精神学习区 | ❌ | 重要思想展示 | 新增 |
| 文章列表 | ✅ | 图文列表形式 | 需完善 |
| 活动展示 | ❌ | 品牌活动展示 | 新增 |
| 公告公示 | ✅ | 侧边栏展示 | 已存在 |
| 协会概况 | ❌ | 侧边栏简介 | 新增 |
| 快捷入口 | ❌ | 重要链接入口 | 新增 |
| 专题推荐 | ❌ | 侧边栏专题 | 新增 |
| 友情链接 | ✅ | 底部/侧边栏 | 已存在 |
| 底部信息 | ⚪️ | 完整底部导航和信息 | 需完善 |
| 公众号二维码 | ✅ | 底部展示 | 已存在 |

### 2.3 导航菜单页面
- 协会概况
- 要闻动态
- 党建工作
- 品牌活动
- 表彰激励
- 公告公示
- 专题
- 全国联动

---

## 三、前端页面需求

### 3.1 首页

#### 3.1.1 顶部区域

**顶部快捷链接栏（新增）**
- 中国文艺网链接
- 中国文联链接
- 其他相关政府/协会网站链接
- 右侧：登录入口、搜索框

**主导航栏**
- Logo展示（中国文艺志愿者协会）
- 导航菜单（8个主菜单）
- 公众号二维码入口

#### 3.1.2 轮播图区（大幅Banner）
- 首页大图轮播（全屏或大幅展示）
- 展示重要活动、会议、精神学习等
- 示例内容：
  - "百花迎春"活动海报
  - "深入学习贯彻党的二十届四中全会精神"
  - 其他重要活动宣传
- 自动轮播+手动切换指示器
- 点击跳转详情页

#### 3.1.3 主体内容区（三栏布局）

**左侧主内容区（60-70%宽度）**

*1. 专题/精神学习区（新增）*
- 大标题展示重要思想/会议精神
- 配图+文字简介
- 点击进入专题详情页
- 示例："深入学习贯彻党的二十届四中全会精神"

*2. 学习贯彻习近平文化思想区（新增）*
- 独立板块展示
- 相关文章列表
- 配图展示

*3. 最新文章/动态列表*
- 图文列表形式展示
- 显示标题、摘要、发布时间
- 支持分页或加载更多
- 包含分类：要闻、资讯、活动等

*4. 活动展示/品牌活动区（新增）*
- 活动图片展示
- 活动标题和时间
- 点击进入活动详情

**右侧边栏（30-40%宽度）**

*1. 公告公示区*
- 最新公告列表
- 显示标题和时间
- 更多链接

*2. 协会概况/简介区（新增）*
- 协会简介文字（简短）
- 协会Logo
- "查看更多"链接

*3. 快速入口/快捷链接区（新增）*
- 重要平台快捷入口
- 示例：中国文艺志愿服务智慧平台

*4. 专题推荐区（新增）*
- 专题封面图
- 专题标题
- 点击进入专题

*5. 友情链接区*
- 相关组织/网站链接

#### 3.1.4 底部区域（Footer）

**底部导航（新增）**
- 网站地图式导航
- 分类链接展示
- 快速访问入口

**底部信息**
- 主办单位：中国文艺志愿者协会
- 承办单位信息
- 联系电话、邮箱
- 办公地址
- ICP备案号
- 版权信息

**公众号展示区**
- 微信公众号二维码
- 扫码关注提示

**访问量统计**
- 网站总访问量
- 各模块访问量统计

---

### 3.2 协会概况页面

**功能需求：**
- 协会介绍（富文本内容）
- 组织架构图
- 领导介绍（姓名、职务、照片）
- 协会章程
- 联系方式
- 办公地址

**数据来源：** 单页内容管理

---

### 3.3 要闻动态页面

**功能需求：**
- 要闻列表（图文列表）
- 分页功能
- 按时间排序（最新在前）
- 搜索功能（标题搜索）
- 点击查看详情
- 上一篇/下一篇导航

**数据来源：** 文章管理（分类：要闻动态）

---

### 3.4 党建工作页面

**功能需求：**
- 党建活动列表
- 党建学习资料
- 党建新闻
- 分页功能
- 搜索功能
- 点击查看详情

**数据来源：** 文章管理（分类：党建工作）

---

### 3.5 品牌活动页面

**功能需求：**
- 活动列表（图文展示）
- 活动时间、地点、参与人数
- 活动照片集
- 活动视频（可选）
- 活动回顾
- 分页功能
- 点击查看详情

**数据来源：** 文章管理（分类：品牌活动）

---

### 3.6 表彰激励页面

**功能需求：**
- 表彰名单
- 获奖人员/单位展示
- 表彰事迹介绍
- 荣誉证书展示
- 分页功能
- 点击查看详情

**数据来源：** 文章管理（分类：表彰激励）

---

### 3.7 公告公示页面

**功能需求：**
- 公告列表
- 显示标题、发布时间
- 重要公告置顶
- 分页功能
- 搜索功能
- 点击查看详情

**数据来源：** 文章管理（分类：公告公示）

---

### 3.8 专题页面

**功能需求：**
- 专题列表（封面+标题）
- 专题详情页
- 专题内文章列表
- 专题介绍
- 点击查看专题内文章

**数据来源：** 专题管理

---

### 3.9 全国联动页面

**功能需求：**
- 全国分会列表
- 各分会链接
- 分会动态
- 分会联系方式
- 地图展示（可选）

**数据来源：** 友情链接管理（分类：全国分会）

---

### 3.10 文章详情页（通用）

**功能需求：**
- 文章标题
- 发布时间、来源、作者
- 文章正文（富文本）
- 文章图片
- 文章附件下载（可选）
- 上一篇/下一篇
- 返回列表
- 浏览量统计

---

## 四、后台管理功能需求

### 4.1 文章管理模块

#### 4.1.1 文章列表
- 文章列表展示（标题、分类、状态、发布时间、浏览量）
- 分页查询
- 搜索（标题、分类）
- 筛选（分类、状态、时间范围）
- 批量操作（删除、下架）
- 排序功能

#### 4.1.2 文章新增/编辑
- 文章标题（必填）
- 文章分类（下拉选择，关联8个导航菜单）
- 文章摘要（可选）
- 文章封面图（上传）
- 文章内容（富文本编辑器）
- 文章来源（可选）
- 作者（可选）
- 发布时间（默认当前时间，可修改）
- 状态（草稿/已发布）
- 是否置顶
- SEO设置（标题、关键词、描述）
- 附件上传（可选）

#### 4.1.3 文章分类管理
- 分类列表
- 分类新增/编辑/删除
- 分类排序
- 分类状态（启用/禁用）
- 分类图标（可选）

**需要新增的分类：**
- 要闻动态
- 党建工作
- 品牌活动
- 表彰激励
- 公告公示
- 专题文章

---

### 4.2 专题管理模块

#### 4.2.1 专题列表
- 专题列表展示（标题、封面、文章数、创建时间）
- 分页查询
- 搜索
- 批量操作

#### 4.2.2 专题新增/编辑
- 专题标题
- 专题封面图
- 专题介绍（富文本）
- 关联文章（多选）
- 排序
- 状态（启用/禁用）

---

### 4.3 友情链接管理模块

#### 4.3.1 友情链接列表
- 链接列表展示（名称、链接、分类、排序）
- 分页查询
- 搜索
- 批量操作

#### 4.3.2 友情链接新增/编辑
- 链接名称
- 链接地址
- 链接Logo（上传）
- 链接分类（普通链接/全国分会）
- 排序
- 状态（启用/禁用）

---

### 4.4 轮播图管理模块（可选）

#### 4.4.1 轮播图列表
- 轮播图列表展示（图片、标题、链接、排序）
- 分页查询
- 批量操作

#### 4.4.2 轮播图新增/编辑
- 轮播图片
- 标题
- 链接地址
- 排序
- 状态（启用/禁用）

---

### 4.5 单页内容管理模块

#### 4.5.1 单页列表
- 单页列表展示（标题、更新时间）
- 搜索

#### 4.5.2 单页编辑
- 协会概况（富文本）
- 组织架构（富文本）
- 领导介绍（富文本）
- 协会章程（富文本）
- 联系方式（富文本）

---

### 4.6 图片管理模块

#### 4.6.1 图片库
- 图片列表（缩略图、文件名、大小、上传时间）
- 文件夹管理
- 批量上传
- 图片预览
- 图片删除
- 图片复制链接

---

### 4.7 访问统计模块

#### 4.7.1 访问量统计
- 网站总访问量（PV/UV）
- 今日访问量
- 本月访问量
- 本年访问量

#### 4.7.2 分模块访问量
- 各页面访问量统计
- 各分类文章访问量
- 访问趋势图表

#### 4.7.3 文章访问统计
- 文章阅读量排行
- 文章浏览量统计
- 文章访问记录

---

### 4.8 快捷链接管理模块（新增）

#### 4.8.1 快捷链接列表
- 链接列表展示（名称、URL、位置、排序）
- 按位置筛选（顶部、侧边栏）
- 分页查询
- 搜索功能
- 批量操作

#### 4.8.2 快捷链接新增/编辑
- 链接名称
- 链接地址
- 链接位置（顶部快捷栏/侧边栏）
- 排序
- 状态（启用/禁用）

---

### 4.9 系统配置模块

#### 4.9.1 站点设置
- 网站标题
- 网站关键词
- 网站描述
- 网站Logo
- 网站Favicon
- ICP备案号
- 联系电话
- 联系邮箱
- 办公地址

#### 4.9.2 公众号设置
- 公众号二维码图片
- 公众号名称
- 公众号简介

#### 4.9.3 底部设置
- 主办单位
- 承办单位
- 底部链接配置
- 底部版权信息
- 底部联系方式
- 备案信息

---

## 五、数据库表设计需求

### 5.1 新增数据表

#### 1. cms_article - 文章表
```sql
CREATE TABLE `cms_article` (
  `article_id` int NOT NULL AUTO_INCREMENT COMMENT '文章ID',
  `title` varchar(200) NOT NULL COMMENT '文章标题',
  `category_id` int NOT NULL COMMENT '分类ID',
  `summary` varchar(500) DEFAULT NULL COMMENT '文章摘要',
  `cover_image` varchar(255) DEFAULT NULL COMMENT '封面图',
  `content` longtext COMMENT '文章内容',
  `source` varchar(100) DEFAULT NULL COMMENT '文章来源',
  `author` varchar(50) DEFAULT NULL COMMENT '作者',
  `publish_time` datetime DEFAULT NULL COMMENT '发布时间',
  `status` char(1) NOT NULL DEFAULT '0' COMMENT '状态：0草稿 1已发布',
  `is_top` char(1) NOT NULL DEFAULT '0' COMMENT '是否置顶：0否 1是',
  `view_count` int NOT NULL DEFAULT '0' COMMENT '浏览量',
  `seo_title` varchar(200) DEFAULT NULL COMMENT 'SEO标题',
  `seo_keywords` varchar(500) DEFAULT NULL COMMENT 'SEO关键词',
  `seo_description` varchar(500) DEFAULT NULL COMMENT 'SEO描述',
  `create_by` varchar(64) NOT NULL DEFAULT '' COMMENT '创建者',
  `create_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT '创建时间',
  `update_by` varchar(64) NOT NULL DEFAULT '' COMMENT '更新者',
  `update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '更新时间',
  `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '删除标志',
  PRIMARY KEY (`article_id`),
  KEY `idx_category_id` (`category_id`),
  KEY `idx_status` (`status`),
  KEY `idx_publish_time` (`publish_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='文章表';
```

#### 2. cms_category - 分类表
```sql
CREATE TABLE `cms_category` (
  `category_id` int NOT NULL AUTO_INCREMENT COMMENT '分类ID',
  `category_name` varchar(50) NOT NULL COMMENT '分类名称',
  `category_code` varchar(50) NOT NULL COMMENT '分类编码',
  `parent_id` int NOT NULL DEFAULT '0' COMMENT '父分类ID',
  `sort_order` int NOT NULL DEFAULT '0' COMMENT '排序',
  `status` char(1) NOT NULL DEFAULT '1' COMMENT '状态：0禁用 1启用',
  `icon` varchar(100) DEFAULT NULL COMMENT '图标',
  `create_by` varchar(64) NOT NULL DEFAULT '' COMMENT '创建者',
  `create_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT '创建时间',
  `update_by` varchar(64) NOT NULL DEFAULT '' COMMENT '更新者',
  `update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '更新时间',
  `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '删除标志',
  PRIMARY KEY (`category_id`),
  UNIQUE KEY `uk_category_code` (`category_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='分类表';
```

#### 3. cms_special - 专题表
```sql
CREATE TABLE `cms_special` (
  `special_id` int NOT NULL AUTO_INCREMENT COMMENT '专题ID',
  `title` varchar(200) NOT NULL COMMENT '专题标题',
  `cover_image` varchar(255) DEFAULT NULL COMMENT '封面图',
  `description` varchar(500) DEFAULT NULL COMMENT '专题描述',
  `sort_order` int NOT NULL DEFAULT '0' COMMENT '排序',
  `status` char(1) NOT NULL DEFAULT '1' COMMENT '状态：0禁用 1启用',
  `create_by` varchar(64) NOT NULL DEFAULT '' COMMENT '创建者',
  `create_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT '创建时间',
  `update_by` varchar(64) NOT NULL DEFAULT '' COMMENT '更新者',
  `update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '更新时间',
  `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '删除标志',
  PRIMARY KEY (`special_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='专题表';
```

#### 4. cms_special_article - 专题文章关联表
```sql
CREATE TABLE `cms_special_article` (
  `id` int NOT NULL AUTO_INCREMENT COMMENT '主键',
  `special_id` int NOT NULL COMMENT '专题ID',
  `article_id` int NOT NULL COMMENT '文章ID',
  PRIMARY KEY (`id`),
  KEY `idx_special_id` (`special_id`),
  KEY `idx_article_id` (`article_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='专题文章关联表';
```

#### 5. cms_link - 友情链接表
```sql
CREATE TABLE `cms_link` (
  `link_id` int NOT NULL AUTO_INCREMENT COMMENT '链接ID',
  `link_name` varchar(100) NOT NULL COMMENT '链接名称',
  `link_url` varchar(255) NOT NULL COMMENT '链接地址',
  `link_logo` varchar(255) DEFAULT NULL COMMENT '链接Logo',
  `link_type` char(1) NOT NULL DEFAULT '0' COMMENT '链接类型：0普通链接 1全国分会',
  `sort_order` int NOT NULL DEFAULT '0' COMMENT '排序',
  `status` char(1) NOT NULL DEFAULT '1' COMMENT '状态：0禁用 1启用',
  `create_by` varchar(64) NOT NULL DEFAULT '' COMMENT '创建者',
  `create_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT '创建时间',
  `update_by` varchar(64) NOT NULL DEFAULT '' COMMENT '更新者',
  `update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '更新时间',
  `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '删除标志',
  PRIMARY KEY (`link_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='友情链接表';
```

#### 6. cms_banner - 轮播图表（可选）
```sql
CREATE TABLE `cms_banner` (
  `banner_id` int NOT NULL AUTO_INCREMENT COMMENT '轮播图ID',
  `title` varchar(200) DEFAULT NULL COMMENT '标题',
  `image` varchar(255) NOT NULL COMMENT '图片',
  `link_url` varchar(255) DEFAULT NULL COMMENT '链接地址',
  `sort_order` int NOT NULL DEFAULT '0' COMMENT '排序',
  `status` char(1) NOT NULL DEFAULT '1' COMMENT '状态：0禁用 1启用',
  `create_by` varchar(64) NOT NULL DEFAULT '' COMMENT '创建者',
  `create_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT '创建时间',
  `update_by` varchar(64) NOT NULL DEFAULT '' COMMENT '更新者',
  `update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '更新时间',
  `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '删除标志',
  PRIMARY KEY (`banner_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='轮播图表';
```

#### 7. cms_page - 单页内容表
```sql
CREATE TABLE `cms_page` (
  `page_id` int NOT NULL AUTO_INCREMENT COMMENT '页面ID',
  `page_key` varchar(50) NOT NULL COMMENT '页面标识',
  `page_name` varchar(100) NOT NULL COMMENT '页面名称',
  `content` longtext COMMENT '内容',
  `create_by` varchar(64) NOT NULL DEFAULT '' COMMENT '创建者',
  `create_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT '创建时间',
  `update_by` varchar(64) NOT NULL DEFAULT '' COMMENT '更新者',
  `update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '更新时间',
  PRIMARY KEY (`page_id`),
  UNIQUE KEY `uk_page_key` (`page_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='单页内容表';
```

#### 8. cms_visit_log - 访问日志表
```sql
CREATE TABLE `cms_visit_log` (
  `log_id` int NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `page_url` varchar(255) NOT NULL COMMENT '访问页面',
  `page_type` varchar(50) NOT NULL COMMENT '页面类型',
  `ref_type_id` int DEFAULT NULL COMMENT '关联ID',
  `ip_address` varchar(128) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` varchar(500) DEFAULT NULL COMMENT '浏览器信息',
  `visit_time` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6) COMMENT '访问时间',
  PRIMARY KEY (`log_id`),
  KEY `idx_visit_time` (`visit_time`),
  KEY `idx_page_type` (`page_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='访问日志表';
```

#### 9. cms_statistics - 统计汇总表
```sql
CREATE TABLE `cms_statistics` (
  `stat_id` int NOT NULL AUTO_INCREMENT COMMENT '统计ID',
  `stat_date` date NOT NULL COMMENT '统计日期',
  `total_pv` int NOT NULL DEFAULT '0' COMMENT '总PV',
  `total_uv` int NOT NULL DEFAULT '0' COMMENT '总UV',
  `article_pv` int NOT NULL DEFAULT '0' COMMENT '文章页PV',
  `article_uv` int NOT NULL DEFAULT '0' COMMENT '文章页UV',
  `home_pv` int NOT NULL DEFAULT '0' COMMENT '首页PV',
  `home_uv` int NOT NULL DEFAULT '0' COMMENT '首页UV',
  `other_pv` int NOT NULL DEFAULT '0' COMMENT '其他页PV',
  `other_uv` int NOT NULL DEFAULT '0' COMMENT '其他页UV',
  PRIMARY KEY (`stat_id`),
  UNIQUE KEY `uk_stat_date` (`stat_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='统计汇总表';
```

#### 10. cms_quick_link - 快捷链接表（新增）
```sql
CREATE TABLE `cms_quick_link` (
  `link_id` int NOT NULL AUTO_INCREMENT COMMENT '链接ID',
  `link_name` varchar(100) NOT NULL COMMENT '链接名称',
  `link_url` varchar(255) NOT NULL COMMENT '链接地址',
  `link_position` varchar(50) NOT NULL COMMENT '链接位置：top顶部 sidebar侧边栏',
  `sort_order` int NOT NULL DEFAULT '0' COMMENT '排序',
  `status` char(1) NOT NULL DEFAULT '1' COMMENT '状态：0禁用 1启用',
  `create_by` varchar(64) NOT NULL DEFAULT '' COMMENT '创建者',
  `create_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT '创建时间',
  `update_by` varchar(64) NOT NULL DEFAULT '' COMMENT '更新者',
  `update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '更新时间',
  `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '删除标志',
  PRIMARY KEY (`link_id`),
  KEY `idx_position` (`link_position`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='快捷链接表';
```

---

## 六、开发优先级

### P0（核心功能，必须实现）
1. 文章管理（新增、编辑、删除、列表）
2. 文章分类管理
3. **轮播图管理（新增）** - 首页大幅Banner
4. **快捷链接管理（新增）** - 顶部和侧边栏快捷入口
5. 前端首页（含轮播图、文章列表、公告、侧边栏）
6. 前端文章详情页
7. 前端各分类页面（要闻动态、党建工作、品牌活动、表彰激励、公告公示）
8. **协会概况展示（新增）** - 侧边栏简介
9. 访问量统计（总访问量、分模块访问量）
10. 友情链接管理
11. 单页内容管理（协会概况）

### P1（重要功能，建议实现）
1. 专题管理
2. **精神学习/重要思想管理（新增）**
3. 全国联动页面
4. 图片管理
5. 公众号二维码设置
6. 站点配置（含主办单位、承办单位、备案信息）
7. 搜索功能
8. **底部导航管理（新增）**

### P2（增强功能，可选实现）
1. 文章置顶功能
2. 文章SEO设置
3. 访问趋势图表
4. 文章附件上传
5. 文章来源/作者管理
6. **活动展示模块（新增）**
7. **专题推荐区（新增）**

---

## 七、技术实现要点

### 7.1 前端技术要点
- 使用Vue3 + Element Plus（已有）
- 富文本编辑器使用@vueup/vue-quill（已有）
- 图片上传使用现有FileUpload组件
- 访问统计使用前端埋点+后端记录

### 7.2 后端技术要点
- 使用NestJS + TypeORM（已有）
- 文章内容使用TEXT类型存储富文本HTML
- 图片存储使用本地存储或OSS
- 访问统计使用中间件拦截记录
- SEO信息存储在文章表中

### 7.3 数据初始化

#### 需要初始化的分类数据：
```sql
INSERT INTO `cms_category` (`category_name`, `category_code`, `parent_id`, `sort_order`, `status`, `create_by`) VALUES
('要闻动态', 'news', 0, 1, '1', 'admin'),
('党建工作', 'party', 0, 2, '1', 'admin'),
('品牌活动', 'activity', 0, 3, '1', 'admin'),
('表彰激励', 'honor', 0, 4, '1', 'admin'),
('公告公示', 'notice', 0, 5, '1', 'admin'),
('专题文章', 'special', 0, 6, '1', 'admin');
```

#### 需要初始化的单页数据：
```sql
INSERT INTO `cms_page` (`page_key`, `page_name`, `content`, `create_by`) VALUES
('association_overview', '协会概况', '', 'admin'),
('organization', '组织架构', '', 'admin'),
('leadership', '领导介绍', '', 'admin'),
('constitution', '协会章程', '', 'admin'),
('contact', '联系方式', '', 'admin');
```

---

## 八、总结

这是一个典型的政府/协会性质官网，核心特点是：

1. **以内容展示为主**，不需要复杂的用户互动功能
2. **固定的栏目结构**，8个主要导航菜单
3. **重视访问统计**，需要总访问量和分模块访问量
4. **需要友情链接**，特别是全国分会链接
5. **需要公众号展示**，方便用户关注

### 相比通用CMS，这个项目不需要：
- ❌ 用户注册/登录
- ❌ 评论功能
- ❌ 点赞/收藏
- ❌ 用户关注
- ❌ 复杂的广告管理
- ❌ 高级SEO功能

### 建议实施计划
- **第一阶段（P0）**：实现核心内容管理功能，快速上线基础版本
- **第二阶段（P1）**：完善用户体验，增加专题、轮播图等功能
- **第三阶段（P2）**：根据实际需求迭代优化，增加增强功能

---

**文档版本：** v1.0  
**创建日期：** 2026-02-04  
**最后更新：** 2026-02-04
